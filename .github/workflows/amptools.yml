name: C++

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PIXI_COLOR: always
  PYTHONHASHSEED: "0"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    paths:
      - "**/*.cc"
      - "**/*.cfg"
      - "**/*.h"
      - "**/Makefile"
      - .github/workflows/amptools.yml
  workflow_dispatch:

jobs:
  build:
    name: Build and execute
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-14
          - ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          submodules: true
      - uses: actions/cache@v4
        with:
          key: binaries-${{ matrix.os }}-${{ hashFiles('**/*.cc', '**/*.h', 'extern/AmpTools', '**/Makefile*') }}
          path: |
            **/*.a
            **/*.d
            **/*.o
            **/compiler_flags
            **/bin
            **/lib
      - uses: prefix-dev/setup-pixi@v0.9.1
        with:
          activate-environment: true
          cache: true
      - if: startsWith(matrix.os, 'macos')
        run: pixi run build -j$(sysctl -n hw.ncpu)
      - if: startsWith(matrix.os, 'ubuntu')
        run: pixi run build -j$(nproc)
      - name: Run executables
        run: |
          ./print_amplitudes -c zlm.cfg > .print_amplitudes.log
          python makeComparison.py
          ./draw_zlm
        working-directory: script
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cpp-logs-${{ matrix.os }}
          path: |
            script/**.log
            script/**.root
            !script/data.root
